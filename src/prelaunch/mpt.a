;license:MIT

!cpu 6502
!to "build/PRELAUNCH.INDEXED/MPT",plain
*=$106

    !source "src/prelaunch/common.a"

         +ENABLE_ACCEL
         lda   #$4C
         sta   $A6A5
         jsr   $8000
         +DISABLE_ACCEL
         jmp   $A6A8

!if * > $1C0 {
  !error "code is too large, ends at ", *
}
